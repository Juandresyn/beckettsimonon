<!doctype html>
<html class="no-js" lang="{{ shop.locale }}">
<head>

  <!-- Hotjar Tracking Code for https://www.beckettsimonon.com/ -->
<script>
    (function(h,o,t,j,a,r){
        h.hj=h.hj||function(){(h.hj.q=h.hj.q||[]).push(arguments)};
        h._hjSettings={hjid:1549162,hjsv:6};
        a=o.getElementsByTagName('head')[0];
        r=o.createElement('script');r.async=1;
        r.src=t+h._hjSettings.hjid+j+h._hjSettings.hjsv;
        a.appendChild(r);
    })(window,document,'https://static.hotjar.com/c/hotjar-','.js?sv=');
</script>

    <!-- END Hotjar Tracking code -->

  <meta name="google-site-verification" content="6PtsEEDcLG2JNxkSvss435wJGoRefHY6qT1Wg7y_Spw" />
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="theme-color" content="{{ settings.browser_color }}">
  <link rel="canonical" href="{{ canonical_url }}">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0" />

  {% if settings.favicon != blank %}
    <link rel="shortcut icon" href="{{ settings.favicon | img_url: '32x32' }}" type="image/png">
  {% endif %}

  {% capture seo_title %}
    {% if template == 'search' and search.performed == true %}
      {{ 'general.search.heading' | t: count: search.results_count }}: {{ 'general.search.results_with_count' | t: terms: search.terms, count: search.results_count }}
    {% else %}
      {{ page_title }}
    {% endif %}
    {% if current_tags %}
      {% assign meta_tags = current_tags | join: ', ' %} &ndash; {{ 'general.meta.tags' | t: tags: meta_tags -}}
    {% endif %}
    {% if current_page != 1 %}
      &ndash; {{ 'general.meta.page' | t: page: current_page }}
    {% endif %}
    {% assign escaped_page_title = page_title | escape %}
    {% unless escaped_page_title contains shop.name %}
      &ndash; {{ shop.name }}
    {% endunless %}
  {% endcapture %}
  <title>{{ seo_title | strip }}</title>

  {% if page_description %}
    <meta name="description" content="{{ page_description | escape }}">
  {% endif %}

  {% include 'social-meta-tags' %}

  <!-- Site Css -->
  {{ 'shop.min.css' | asset_url | stylesheet_tag }}
  <!-- END Site Css -->

  {% if template.directory == 'customers' %}
    <script src="{{ 'shopify_common.js' | shopify_asset_url }}" defer="defer"></script>
  {% endif %}

  <script src="{{ 'theme.js' | asset_url }}" defer="defer"></script>
  <script src="{{ 'beckett-simonon.min.js' | asset_url }}" defer="defer"></script>
  <script
    src="https://code.jquery.com/jquery-3.3.1.min.js"
    integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8="
    crossorigin="anonymous"></script>
  <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>

  <!-- Slick Slider -->
  <link rel="stylesheet" type="text/css" href="//cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.css"/>
  <script type="text/javascript" src="//cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.min.js"></script>
  <!-- END Slick Slider -->

  <!-- Lazyload -->
  <script src="https://cdn.jsdelivr.net/npm/vanilla-lazyload@12.0.0/dist/lazyload.min.js"></script>
  <!-- END Lazyload -->

  {{ content_for_header }}

  <!-- Product Colors -->
  <style>
    {{ settings.product_colors_css }}
  </style>
  <!-- End Product Colors -->

  <!-- Okendo Reviews Css -->
    {{ shop.metafields.okendo.ReviewsWidgetStyleTag }}
    {{ shop.metafields.okendo.StyleCustomizations }}
  <!-- Okendo Reviews Css -->

	<!-- Begin Pinterest Tag -->
      <script>
      !function(e){if(!window.pintrk){window.pintrk=function()
      {window.pintrk.queue.push(Array.prototype.slice.call(arguments))};var
      n=window.pintrk;n.queue=[],n.version="3.0";var
      t=document.createElement("script");t.async=!0,t.src=e;var
      r=document.getElementsByTagName("script")[0];r.parentNode.insertBefore(t,r)}}
      ("https://s.pinimg.com/ct/core.js");

      pintrk('load','2620341494974', { em: '{{ customer.email }}', np: 'generator' , });
      pintrk('page');
      </script>
      <!-- Pinterest Events -->
      <script>
      //addtocart event
      window.addEventListener('load', function() {
      if (document.getElementsByClassName('btn btn--scarlet product-header__button js-add-to-cart-btn')) {
      var ATCbutton = document.getElementsByClassName('btn btn--scarlet product-header__button js-add-to-cart-btn');
      for (var i = 0; i < ATCbutton.length; i++) {
      ATCbutton[i].addEventListener('click', function() {
      pintrk('track', 'addtocart',{
      np: 'generator',
      });
      })}}});

      //signup event
      window.addEventListener('load', function() {
      if (document.getElementsByClassName('btn btn--scarlet login-form-modal__btn')) {
      var ATCbutton = document.getElementsByClassName('btn btn--scarlet login-form-modal__btn');
      for (var i = 0; i < ATCbutton.length; i++) {
      ATCbutton[i].addEventListener('click', function() {
      pintrk('track', 'signup',{
      np: 'generator',
      });
      })}}});

      //lead event
      window.addEventListener('load', function() {
      if (document.getElementsByClassName('icon icon--arrow animation--go-to')) {
      var ATCbutton = document.getElementsByClassName('icon icon--arrow animation--go-to');
      for (var i = 0; i < ATCbutton.length; i++) {
      ATCbutton[i].addEventListener('click', function() {
      pintrk('track', 'lead',{
      np: 'generator',
      });
      })}}});
      </script>
	<!-- End Pinterest Tag -->
</head>

{% unless template contains 'cart' %}
  {% assign nav_class = "has-sticky-nav" %}
{% endunless %}

{% if template == 'page' %}
  {% assign pageClass = 'page--generic' %}
{% endif %}

{% if template contains 'page.information' %}
  {% assign pageClass = 'page--information' %}
{% endif %}

{% if template contains 'page.contact' %}
  {% assign pageClass = 'page--contact' %}
{% endif %}

{% if template contains 'page.affiliates' %}
  {% assign pageClass = 'page--affiliates' %}
{% endif %}

{% if template == blank %}
  {% assign pageClass = 'page--empty' %}
  {% assign isEmpty = true %}
{% endif %}

<body id="theme-content" class="page--{{ template | replace: '/', '-' | split: '.' | first }} {{ pageClass }} {{ nav_class }}">
  {% section 'header' %}

  <main class="main-content" id="MainContent" role="main" tabindex="-1">
    {{ content_for_layout }}
  </main>

  {% assign is_customer = true %}
  {% assign header_classes = "header-main--small" %}
  {% unless template contains "404"
    or template contains "account"
    or template contains "order"
    or template contains "cart" %}
    {% unless template contains "page.information"
      or isEmpty
      or template contains "page.contact"
      or template contains "collection"
      or template contains "page.affiliates"
      or template contains "blog"
      or template contains "customer"
      or template contains "search"
      or template contains "article" %}
      {% include 'press-carousel' %}
    {% endunless %}

    {% section 'footer' %}
  {% endunless %}

  {% unless template contains "cart" %}
    {% section 'end-of-page' %}
  {% endunless %}

  {% include 'inline-cart' %}
  {% include 'search-modal' %}
  {% include 'mega-menu' %}

  <script type="text/javascript">
    var initTemplate = function() {
      if (window.template) {
        window.template.init();
      } else {
        setTimeout(function() {
          initTemplate();
        }, 500);
      }
    };

    $(window).on('load', initTemplate());

    const cartItems = {{ cart.items | json }};
    cartItems.forEach(i =>
        i.material = '');
    window.cart = cartItems;
    window.cart_count = {{ cart.item_count }};
    window.template_name = "{{ template }}";

    document.documentElement.className = document.documentElement.className.replace('no-js', 'js');
  </script>

  <script src="https://cdn.jsdelivr.net/npm/vue"></script>
  <script src="https://fast.wistia.com/assets/external/E-v1.js" async></script>
</body>
</html>
